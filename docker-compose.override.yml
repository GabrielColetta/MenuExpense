version: '3'

services:
  menu.controller:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"
  menu.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"
networks:
  default:
    external:
      name: nat

